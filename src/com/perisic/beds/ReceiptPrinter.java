package com.perisic.beds;

/**
 * @author Marc Conrad
 *
 */
public class ReceiptPrinter {
	/**
	 * Print Method -- receives a string object 
	 * and call the system standard output.
	 * Prints the string which is the receipt into the java console
	 * @param str
	 */
	public void print(String str) { 
		System.out.println(str);
	}
}
